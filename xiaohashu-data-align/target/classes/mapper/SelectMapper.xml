<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quanxiaoha.xiaohashu.data.align.domain.mapper.SelectMapper">
    <select id="selectBatchFromDataAlignFollowingCountTempTable" resultType="java.lang.Long">
        select user_id from `t_data_align_following_count_temp_${tableNameSuffix}` order by id
        limit #{batchSize}
    </select>
    <select id="selectCountFromFollowingTableByUserId" resultType="java.lang.Integer">
        select count(*) from t_following where user_id = #{userId}
    </select>
    <select id="selectBatchFromDataAlignNotePublishCountTempTable" resultType="java.lang.Long">
        select user_id from `t_data_align_note_publish_count_temp_${tableNameSuffix}` order by id
            limit #{batchSize}
    </select>
    <select id="selectCountFromNotePublishTableByUserId" resultType="java.lang.Integer">
        select count(*) from t_note where creator_id = #{userId}
    </select>
    <select id="selectBatchFromDataAlignFansCountTempTable" resultType="java.lang.Long">
        select user_id from `t_data_align_fans_count_temp_${tableNameSuffix}` order by id
            limit #{batchSize}
    </select>
    <select id="selectCountFromFansTableByUserId" resultType="java.lang.Integer">
        select count(*) from t_fans where user_id = #{userId}
    </select>
    <select id="selectBatchFromDataAlignNoteLikeCountTempTable" resultType="java.lang.Long">
        select note_id from `t_data_align_note_like_count_temp_${tableNameSuffix}` order by id
            limit #{batchSize}
    </select>
    <select id="selectCountFromNoteLikeTableByUserId" resultType="java.lang.Integer">
        select count(*) from t_note_like where note_id = #{noteId}
    </select>
    <select id="selectBatchFromDataAlignNoteCollectCountTempTable" resultType="java.lang.Long">
        select note_id from `t_data_align_note_collect_count_temp_${tableNameSuffix}` order by id
            limit #{batchSize}
    </select>
    <select id="selectCountFromNoteCollectTableByUserId" resultType="java.lang.Integer">
        select count(*) from t_note_collection where note_id = #{noteId}
    </select>


</mapper>